# Pinned session — 2026-04-26

This research repo was created from a Claude Code session that ran
on 2026-04-26 in `~/ai_repo/remove-tg-bots`. The session JSONL was
copied to this repo's project directory so it can be resumed from
inside `~/ai_repo/statusline`.

## Where the transcript lives

```
~/.claude/projects/-Users-likhomanenkowork-ai-repo-statusline/
└── 44c19bae-d9ba-455f-8294-7092a03f249f.jsonl
```

To resume the conversation in Claude Code, open Claude Code with
`~/ai_repo/statusline` as the working directory and run:

```
/resume 44c19bae-d9ba-455f-8294-7092a03f249f
```

Or list available sessions with `/resume`.

## What the session covered

1. **Stage 1** — broad plan for adding remaining day/week tokens to the
   Claude Code status line. Surfaced the official `rate_limits` stdin
   field and its limitations (Pro/Max only, blank before first API
   response).
2. **Stage 2** — explored two reference repos
   (`sirmalloc/ccstatusline`, `AlexDobrushskiy/botfarm`). Discovered
   the undocumented `api.anthropic.com/api/oauth/usage` endpoint and
   the OAuth-bearer credential paths (Keychain + `.credentials.json`).
   Updated the plan into A / B / C options.
3. **Stage 3** — security review of `ccstatusline`. Found one open
   supply-chain issue (#298, `npx -y …@latest`), historical fixes
   (#156 directory traversal, `CLAUDE_CONFIG_DIR` injection, TOCTOU),
   and code-level concerns (`dump-keychain` enumeration,
   `CustomCommand` env leak). Decision recommended: Option B with
   `find-generic-password`-only credential lookup.

## What did **not** make it into this repo

- Memory entries from the original session — those were rmbots-specific
  and left alone in the source project.
- The `67e9ef4f-…` session JSONL from earlier in the same day — that
  was an unrelated rmbots-work session, not part of this research.
